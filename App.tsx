import React from "react";
import {View, Text, TextInput, StyleSheet} from 'react-native'

import Login from '../FrotaANE/src/pages/login'

export default function App(){
  return(
    <View>
      <Login/>
    </View>
  )
}
const styles = StyleSheet.create({
  container: {
  },
});

